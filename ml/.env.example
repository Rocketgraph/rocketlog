# Copy to .env and fill in credentials for the sources you want to enable.
# Any subset works — you only need to configure the sources you'll query.
# You can also leave .env blank and POST /credentials at runtime.

# ── New Relic ────────────────────────────────────────────────────────────────
# NR_API_KEY=NRAK-XXXXXXXXXXXXXXXXXXXX
# NR_ACCOUNT_ID=1234567

# ── Loki ─────────────────────────────────────────────────────────────────────
# LOKI_URL=https://loki.example.com
# LOKI_API_KEY=your-bearer-token
# LOKI_QUERY={service_name=~".+"}

# ── Datadog ──────────────────────────────────────────────────────────────────
# DD_API_KEY=
# DD_APP_KEY=
# DD_SITE=datadoghq.com         # datadoghq.eu | us3.datadoghq.com | ddog-gov.com
# DD_QUERY=*

# ── AWS CloudWatch ───────────────────────────────────────────────────────────
# CW_ACCESS_KEY_ID=
# CW_SECRET_ACCESS_KEY=
# CW_REGION=us-east-1
# CW_LOG_GROUP=/aws/lambda/my-function
# CW_LOG_STREAM=                 # optional

# ── Sentry ───────────────────────────────────────────────────────────────────
# SENTRY_AUTH_TOKEN=sntrys_...
# SENTRY_ORG=my-org
# SENTRY_PROJECT=my-project

# ── ClickHouse ───────────────────────────────────────────────────────────────
# CH_URL=http://clickhouse:8123
# CH_USER=default
# CH_PASSWORD=
# CH_DATABASE=default
# CH_TABLE=logs
# CH_COL_TIMESTAMP=timestamp
# CH_COL_MESSAGE=message
# CH_COL_LEVEL=level
# CH_COL_SERVICE=service

# ── Log file (.log / .json / .csv on disk) ───────────────────────────────────
# FILE_PATH=/data/app.log       # path to the file (mount it into the container)
# FILE_FORMAT=auto              # auto | json | csv | text
# FILE_SERVICE=                 # optional fallback service name (default: file stem)

# ── AI triage (optional — used by example-setups/logfile-quickstart/analyze.py) ─
# ANTHROPIC_API_KEY=sk-ant-...   # sends the cluster output to Claude for plain-English triage

# ── ML knobs ─────────────────────────────────────────────────────────────────
# DRAIN_SIM_TH=0.4
# ANOMALY_CONTAMINATION=0.1
# HST_THRESHOLD=0.7
# DEFAULT_LOOKBACK_HOURS=6
# MAX_ROWS=100000
